#pragma once
/*#include <SFML/Window.hpp>
#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>
#include <SFML/System.hpp>

class SpriteTextureLoading
{
public:
   //Background images to be used in the game
    sf::Texture background;
    sf::Texture startScreen;

    sf::Sprite backgroundSprite;
    sf::Sprite startSprite;

    //Textures for the enemy animations
    sf::Texture greenEnemy1;
    sf::Texture greenEnemy2;

    sf::Texture redEnemy1;
    sf::Texture redEnemy2;

    sf::Texture yellowEnemy1;
    sf::Texture yellowEnemy2;

    sf::Texture redEnemy[2];
    sf::Texture greenEnemy[2];
    sf::Texture yellowEnemy[2];

    //Explosion sprites for enemy death animation
    sf::Texture explosion[6];

    //Font and text to display score and player information
    sf::Font galagaFont;
    sf::Text score;
    sf::Text currentScore;
    sf::Text lossMessage;
    sf::Text winMessage;
    int currentScoreNumber = 0;

    //Ship and missile sprites
    sf::Texture ship;
    sf::Texture hurtShip;
    sf::Texture missile;

    //Sound effect data to provide feedback in game
    sf::SoundBuffer shootBuffer;
    sf::SoundBuffer deathBuffer;
    sf::SoundBuffer startBuffer;
    sf::SoundBuffer winBuffer;
    sf::SoundBuffer loseBuffer;

    sf::Sound shootSound;
    sf::Sound deathSound;
    sf::Sound startSound;
    sf::Sound winSound;
    sf::Sound lossSound;

    //Function that initializes all needed values
    SpriteTextureLoading();
};

*/